---
phase: 01-foundation-project-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - postcss.config.mjs
  - next.config.ts
  - eslint.config.mjs
  - vitest.config.ts
  - trigger.config.ts
  - .env.example
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/types/analysis.ts
  - src/types/competitor.ts
  - src/types/viral.ts
  - src/types/database.ts
  - src/config/apify.ts
  - src/config/gemini.ts
  - src/config/supabase.ts
  - src/config/bunny.ts
  - src/config/trigger.ts
  - src/config/assemblyai.ts
  - src/utils/formatters.ts
  - src/utils/validators.ts
autonomous: true
requirements:
  - FOUND-01
  - FOUND-04
  - FOUND-06

must_haves:
  truths:
    - "Running `npm run dev` starts the Next.js app on localhost with a visible page"
    - "All TypeScript domain types exist and are importable with zero `any` usage"
    - "All environment variables have a .env.example entry and Zod-validated config file"
    - "Project uses Tailwind v4 CSS-first config (no tailwind.config.js)"
    - "Vitest and ESLint are configured and runnable"
  artifacts:
    - path: "package.json"
      provides: "All dependencies with correct versions"
      contains: "@google/genai"
    - path: "src/types/analysis.ts"
      provides: "Analysis, AnalysisInput, NicheInterpreted types"
      exports: ["Analysis", "AnalysisInput", "AnalysisStatus", "AnalysisMode", "NicheInterpreted"]
    - path: "src/types/competitor.ts"
      provides: "Competitor and sub-data types"
      exports: ["Competitor", "WebsiteData", "SeoData", "SocialData", "MetaAdsData", "GoogleAdsData", "GmbData"]
    - path: "src/types/viral.ts"
      provides: "ViralContent, EngagementMetrics, HookBodyCta types"
      exports: ["ViralContent", "ContentPlatform", "EngagementMetrics", "HookBodyCta"]
    - path: "src/types/database.ts"
      provides: "Database row types matching Supabase schema"
      exports: ["Database", "Tables"]
    - path: "src/config/supabase.ts"
      provides: "Zod-validated Supabase config"
      exports: ["supabaseConfig"]
    - path: ".env.example"
      provides: "All 14 environment variable entries"
      min_lines: 14
  key_links:
    - from: "src/config/supabase.ts"
      to: "zod"
      via: "Zod schema parse for env validation"
      pattern: "z\\.object"
    - from: "src/types/database.ts"
      to: "src/types/analysis.ts"
      via: "Re-exports domain types as DB row shapes"
      pattern: "import.*from.*analysis"
---

<objective>
Scaffold the Next.js project with all dependencies, create the complete folder structure, define all TypeScript domain types, and set up environment configuration with Zod validation.

Purpose: This is the absolute foundation. Every subsequent plan and phase depends on the project existing, types being importable, and config being validated.
Output: A running Next.js app with all deps installed, domain types defined, env config validated, and the full folder structure in place.
</objective>

<execution_context>
@C:/Users/biela/OneDrive/Documentos/github/lupai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/biela/OneDrive/Documentos/github/lupai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-project-setup/01-CONTEXT.md
@.planning/phases/01-foundation-project-setup/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project and install all dependencies</name>
  <files>package.json, tsconfig.json, postcss.config.mjs, next.config.ts, eslint.config.mjs, vitest.config.ts, trigger.config.ts, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <read_first>CLAUDE.md, .planning/phases/01-foundation-project-setup/01-RESEARCH.md</read_first>
  <action>
1. Scaffold the project using create-next-app (per D-01):
   ```
   npx create-next-app@15 . --typescript --tailwind --eslint --app --src-dir --turbopack --use-npm
   ```
   Note: If the directory is not empty (due to .planning, CLAUDE.md etc), the scaffolder may prompt. In that case, manually create the equivalent files.

2. Install production dependencies (per D-02, D-03, D-04, D-16, D-17, D-18):
   ```
   npm install @supabase/supabase-js@2 @google/genai assemblyai apify-client@2 @trigger.dev/sdk @trigger.dev/react-hooks zod@3 zod-to-json-schema lucide-react
   ```

3. Install dev dependencies (per D-05):
   ```
   npm install -D vitest @vitejs/plugin-react vite-tsconfig-paths @testing-library/react @testing-library/dom @types/react @types/node
   ```

4. Add Zod override to package.json to prevent transitive Zod v4:
   ```json
   "overrides": { "zod": "3.25.76" }
   ```

5. Add scripts to package.json:
   ```json
   "scripts": {
     "dev": "next dev --turbopack",
     "build": "next build",
     "start": "next start",
     "lint": "next lint",
     "typecheck": "tsc --noEmit",
     "test": "vitest run",
     "test:watch": "vitest"
   }
   ```

6. Ensure tsconfig.json has path alias `@/*` pointing to `./src/*`.

7. Create `postcss.config.mjs` (per D-01, Tailwind v4 CSS-first):
   ```javascript
   const config = {
     plugins: {
       "@tailwindcss/postcss": {},
     },
   };
   export default config;
   ```

8. Update `src/app/globals.css` to Tailwind v4 format:
   ```css
   @import "tailwindcss";

   @theme {
     --color-brand: #6366f1;
     --color-brand-light: #818cf8;
     --font-sans: 'Inter', system-ui, sans-serif;
   }
   ```

9. Update `src/app/layout.tsx` with PT-BR lang attribute:
   ```typescript
   import type { Metadata } from 'next';
   import './globals.css';

   export const metadata: Metadata = {
     title: 'LupAI - Inteligencia de Marketing com IA',
     description: 'Analise completa do mercado e nicho com IA',
   };

   const RootLayout = ({ children }: { children: React.ReactNode }) => {
     return (
       <html lang="pt-BR">
         <body className="antialiased">{children}</body>
       </html>
     );
   };

   export default RootLayout;
   ```

10. Update `src/app/page.tsx` with a minimal placeholder:
    ```typescript
    const HomePage = () => {
      return (
        <main className="flex min-h-screen flex-col items-center justify-center p-8">
          <h1 className="text-4xl font-bold text-brand">LupAI</h1>
          <p className="mt-4 text-lg text-gray-600">
            Inteligencia de marketing com IA
          </p>
        </main>
      );
    };

    export default HomePage;
    ```

11. Create `vitest.config.ts`:
    ```typescript
    import { defineConfig } from 'vitest/config';
    import react from '@vitejs/plugin-react';
    import tsconfigPaths from 'vite-tsconfig-paths';

    export default defineConfig({
      plugins: [react(), tsconfigPaths()],
      test: {
        environment: 'node',
        globals: true,
        include: ['tests/**/*.test.ts', 'tests/**/*.test.tsx', 'src/**/*.test.ts'],
      },
    });
    ```

12. Create `trigger.config.ts` at project root:
    ```typescript
    import { defineConfig } from "@trigger.dev/sdk";

    export default defineConfig({
      project: "lupai",
      dirs: ["src/trigger"],
    });
    ```

13. Create the complete folder structure per D-06 and CLAUDE.md. Create placeholder/stub files for directories that need them:
    - `src/components/ui/.gitkeep`
    - `src/components/analysis/.gitkeep`
    - `src/components/competitors/.gitkeep`
    - `src/components/viral/.gitkeep`
    - `src/components/report/.gitkeep`
    - `src/hooks/.gitkeep`
    - `src/trigger/example.ts` (minimal placeholder task)
    - `src/lib/pdf/generate.ts` (placeholder export)
    - `src/utils/formatters.ts` (placeholder export)

14. For `src/trigger/example.ts`, create a minimal Trigger.dev task placeholder:
    ```typescript
    import { task } from "@trigger.dev/sdk";

    /** Tarefa de exemplo do Trigger.dev - sera substituida por tarefas reais */
    export const exampleTask = task({
      id: "example-task",
      run: async () => {
        return { message: "Trigger.dev configurado com sucesso" };
      },
    });
    ```

15. For `src/utils/formatters.ts`:
    ```typescript
    /** Formata data para exibicao em PT-BR */
    export const formatDatePtBr = (date: Date | string): string => {
      const d = typeof date === 'string' ? new Date(date) : date;
      return d.toLocaleDateString('pt-BR', {
        day: '2-digit',
        month: '2-digit',
        year: 'numeric',
      });
    };
    ```

16. For `src/lib/pdf/generate.ts`:
    ```typescript
    /** Placeholder para geracao de PDF - implementado na Phase 10 */
    export const generatePdf = async (): Promise<void> => {
      throw new Error('Geracao de PDF ainda nao implementada');
    };
    ```

17. Delete `tailwind.config.ts` or `tailwind.config.js` if create-next-app generated it (Tailwind v4 uses CSS-first config per D-01).
  </action>
  <verify>
    <automated>npm run dev -- --port 3099 &amp; sleep 5 &amp;&amp; curl -s http://localhost:3099 | grep -q "LupAI" &amp;&amp; echo "PASS: dev server running" &amp;&amp; kill %1 || (kill %1 2>/dev/null; echo "FAIL")</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"@google/genai"` (NOT `@google/generative-ai`) per D-03
    - package.json contains `"@trigger.dev/sdk"` and `"@trigger.dev/react-hooks"`
    - package.json contains `"overrides"` with `"zod"` key
    - package.json contains `"zod-to-json-schema"`
    - package.json scripts include `"test": "vitest run"` and `"typecheck": "tsc --noEmit"`
    - postcss.config.mjs contains `"@tailwindcss/postcss"`
    - No file named `tailwind.config.ts` or `tailwind.config.js` exists
    - src/app/globals.css contains `@import "tailwindcss"` and `@theme`
    - src/app/layout.tsx contains `lang="pt-BR"`
    - src/app/page.tsx contains `LupAI`
    - vitest.config.ts contains `tsconfigPaths`
    - trigger.config.ts contains `dirs: ["src/trigger"]`
    - tsconfig.json contains `"@/*": ["./src/*"]`
    - src/trigger/example.ts contains `task({`
    - Directories exist: src/components/ui, src/components/analysis, src/components/competitors, src/components/viral, src/components/report, src/hooks, src/lib/pdf
  </acceptance_criteria>
  <done>Next.js app starts with `npm run dev`, all dependencies installed with correct versions, Tailwind v4 CSS-first config active, Vitest and ESLint configured, complete folder structure in place, Trigger.dev config present.</done>
</task>

<task type="auto">
  <name>Task 2: Define all TypeScript domain types and environment config</name>
  <files>src/types/analysis.ts, src/types/competitor.ts, src/types/viral.ts, src/types/database.ts, src/config/apify.ts, src/config/gemini.ts, src/config/supabase.ts, src/config/bunny.ts, src/config/trigger.ts, src/config/assemblyai.ts, src/utils/validators.ts, .env.example</files>
  <read_first>CLAUDE.md, .planning/phases/01-foundation-project-setup/01-CONTEXT.md, .planning/phases/01-foundation-project-setup/01-RESEARCH.md, src/types/analysis.ts, src/types/competitor.ts, src/types/viral.ts, src/types/database.ts</read_first>
  <action>
1. Create `src/types/analysis.ts` (per D-04, D-07):
   ```typescript
   /** Modos de analise disponiveis */
   export type AnalysisMode = 'quick' | 'complete';

   /** Status possiveis de uma analise */
   export type AnalysisStatus = 'pending' | 'processing' | 'completed' | 'failed';

   /** Interpretacao do nicho pela IA */
   export interface NicheInterpreted {
     niche: string;
     segment: string;
     region: string;
   }

   /** Input do usuario para iniciar uma analise */
   export interface AnalysisInput {
     nicheInput: string;
     mode: AnalysisMode;
     userBusinessUrl?: string | null;
   }

   /** Analise completa como retornada do banco */
   export interface Analysis {
     id: string;
     nicheInput: string;
     nicheInterpreted: NicheInterpreted | null;
     mode: AnalysisMode;
     status: AnalysisStatus;
     userBusinessUrl: string | null;
     triggerRunId: string | null;
     createdAt: string;
     updatedAt: string;
   }

   /** Campos atualizaveis de uma analise */
   export interface AnalysisUpdate {
     nicheInterpreted?: NicheInterpreted;
     status?: AnalysisStatus;
     triggerRunId?: string;
   }
   ```

2. Create `src/types/competitor.ts` (per D-04, D-08, D-11):
   ```typescript
   /** Dados extraidos do site do concorrente */
   export interface WebsiteData {
     positioning: string | null;
     offer: string | null;
     pricing: string | null;
     metaTags: {
       title: string | null;
       description: string | null;
       keywords: string[];
     };
   }

   /** Dados de SEO do concorrente */
   export interface SeoData {
     estimatedAuthority: number | null;
     topKeywords: string[];
     estimatedTraffic: number | null;
     backlinks: number | null;
   }

   /** Dados de redes sociais do concorrente */
   export interface SocialData {
     instagram: {
       followers: number | null;
       postingFrequency: string | null;
       engagementRate: number | null;
       topPosts: SocialPost[];
     } | null;
     tiktok: {
       followers: number | null;
       postingFrequency: string | null;
       engagementRate: number | null;
       topPosts: SocialPost[];
     } | null;
   }

   /** Post de rede social */
   export interface SocialPost {
     url: string;
     caption: string | null;
     likes: number;
     comments: number;
     shares: number | null;
     postedAt: string | null;
   }

   /** Dados de anuncios Meta do concorrente */
   export interface MetaAdsData {
     activeAdsCount: number;
     ads: MetaAd[];
   }

   /** Anuncio individual do Meta */
   export interface MetaAd {
     adId: string;
     creativeUrl: string | null;
     copyText: string | null;
     format: string | null;
     startedAt: string | null;
     isActive: boolean;
   }

   /** Dados de Google Ads do concorrente */
   export interface GoogleAdsData {
     hasSearchAds: boolean;
     paidKeywords: string[];
     estimatedBudget: string | null;
   }

   /** Dados do Google Meu Negocio do concorrente */
   export interface GmbData {
     name: string | null;
     rating: number | null;
     reviewCount: number | null;
     address: string | null;
     phone: string | null;
     categories: string[];
   }

   /** Concorrente completo como retornado do banco */
   export interface Competitor {
     id: string;
     analysisId: string;
     name: string;
     websiteUrl: string | null;
     websiteData: WebsiteData | null;
     seoData: SeoData | null;
     socialData: SocialData | null;
     metaAdsData: MetaAdsData | null;
     googleAdsData: GoogleAdsData | null;
     gmbData: GmbData | null;
     createdAt: string;
   }

   /** Input para criar um concorrente */
   export interface CompetitorInput {
     analysisId: string;
     name: string;
     websiteUrl?: string | null;
   }
   ```

3. Create `src/types/viral.ts` (per D-04, D-09):
   ```typescript
   /** Plataformas de conteudo viral */
   export type ContentPlatform = 'tiktok' | 'instagram' | 'facebook';

   /** Metricas de engajamento */
   export interface EngagementMetrics {
     views: number | null;
     likes: number;
     comments: number;
     shares: number | null;
     saves: number | null;
   }

   /** Estrutura gancho-corpo-CTA de um video */
   export interface HookBodyCta {
     hook: string;
     body: string;
     cta: string;
     hookDurationSeconds: number | null;
     totalDurationSeconds: number | null;
   }

   /** Conteudo viral como retornado do banco */
   export interface ViralContent {
     id: string;
     analysisId: string;
     platform: ContentPlatform;
     sourceUrl: string;
     bunnyUrl: string | null;
     transcription: string | null;
     hookBodyCta: HookBodyCta | null;
     engagementMetrics: EngagementMetrics;
     createdAt: string;
   }

   /** Input para criar conteudo viral */
   export interface ViralContentInput {
     analysisId: string;
     platform: ContentPlatform;
     sourceUrl: string;
     engagementMetrics: EngagementMetrics;
   }
   ```

4. Create `src/types/database.ts` (per D-04, D-07 through D-10) — Supabase-compatible Database type:
   ```typescript
   import type { Analysis, AnalysisMode, AnalysisStatus, NicheInterpreted } from './analysis';
   import type { Competitor, WebsiteData, SeoData, SocialData, MetaAdsData, GoogleAdsData, GmbData } from './competitor';
   import type { ViralContent, ContentPlatform, EngagementMetrics, HookBodyCta } from './viral';

   /** Recomendacao estrategica gerada pela IA */
   export interface Recommendation {
     title: string;
     description: string;
     priority: 'high' | 'medium' | 'low';
     category: string;
   }

   /** Roteiro criativo gerado pela IA */
   export interface CreativeScript {
     title: string;
     hook: string;
     body: string;
     cta: string;
     format: string;
     estimatedDurationSeconds: number;
     platform: string;
   }

   /** Analise comparativa (Modo Completo) */
   export interface ComparativeAnalysis {
     userBusinessData: Record<string, unknown>;
     comparisons: Array<{
       competitorId: string;
       competitorName: string;
       insights: string[];
     }>;
     personalizedRecommendations: string[];
   }

   /** Sintese como retornada do banco */
   export interface Synthesis {
     id: string;
     analysisId: string;
     strategicOverview: string;
     recommendations: Recommendation[];
     creativeScripts: CreativeScript[];
     comparativeAnalysis: ComparativeAnalysis | null;
     createdAt: string;
   }

   /** Tipo Database para Supabase client generico */
   export interface Database {
     public: {
       Tables: {
         analyses: {
           Row: {
             id: string;
             niche_input: string;
             niche_interpreted: NicheInterpreted | null;
             mode: AnalysisMode;
             status: AnalysisStatus;
             user_business_url: string | null;
             trigger_run_id: string | null;
             created_at: string;
             updated_at: string;
           };
           Insert: {
             id?: string;
             niche_input: string;
             niche_interpreted?: NicheInterpreted | null;
             mode?: AnalysisMode;
             status?: AnalysisStatus;
             user_business_url?: string | null;
             trigger_run_id?: string | null;
           };
           Update: {
             niche_interpreted?: NicheInterpreted | null;
             mode?: AnalysisMode;
             status?: AnalysisStatus;
             user_business_url?: string | null;
             trigger_run_id?: string | null;
           };
         };
         competitors: {
           Row: {
             id: string;
             analysis_id: string;
             name: string;
             website_url: string | null;
             website_data: WebsiteData | null;
             seo_data: SeoData | null;
             social_data: SocialData | null;
             meta_ads_data: MetaAdsData | null;
             google_ads_data: GoogleAdsData | null;
             gmb_data: GmbData | null;
             created_at: string;
           };
           Insert: {
             id?: string;
             analysis_id: string;
             name: string;
             website_url?: string | null;
             website_data?: WebsiteData | null;
             seo_data?: SeoData | null;
             social_data?: SocialData | null;
             meta_ads_data?: MetaAdsData | null;
             google_ads_data?: GoogleAdsData | null;
             gmb_data?: GmbData | null;
           };
           Update: {
             name?: string;
             website_url?: string | null;
             website_data?: WebsiteData | null;
             seo_data?: SeoData | null;
             social_data?: SocialData | null;
             meta_ads_data?: MetaAdsData | null;
             google_ads_data?: GoogleAdsData | null;
             gmb_data?: GmbData | null;
           };
         };
         viral_content: {
           Row: {
             id: string;
             analysis_id: string;
             platform: ContentPlatform;
             source_url: string;
             bunny_url: string | null;
             transcription: string | null;
             hook_body_cta: HookBodyCta | null;
             engagement_metrics: EngagementMetrics;
             created_at: string;
           };
           Insert: {
             id?: string;
             analysis_id: string;
             platform: ContentPlatform;
             source_url: string;
             bunny_url?: string | null;
             transcription?: string | null;
             hook_body_cta?: HookBodyCta | null;
             engagement_metrics: EngagementMetrics;
           };
           Update: {
             platform?: ContentPlatform;
             source_url?: string;
             bunny_url?: string | null;
             transcription?: string | null;
             hook_body_cta?: HookBodyCta | null;
             engagement_metrics?: EngagementMetrics;
           };
         };
         synthesis: {
           Row: {
             id: string;
             analysis_id: string;
             strategic_overview: string;
             recommendations: Recommendation[];
             creative_scripts: CreativeScript[];
             comparative_analysis: ComparativeAnalysis | null;
             created_at: string;
           };
           Insert: {
             id?: string;
             analysis_id: string;
             strategic_overview: string;
             recommendations: Recommendation[];
             creative_scripts: CreativeScript[];
             comparative_analysis?: ComparativeAnalysis | null;
           };
           Update: {
             strategic_overview?: string;
             recommendations?: Recommendation[];
             creative_scripts?: CreativeScript[];
             comparative_analysis?: ComparativeAnalysis | null;
           };
         };
       };
     };
   }

   /** Helper type para acessar tabelas */
   export type Tables<T extends keyof Database['public']['Tables']> = Database['public']['Tables'][T]['Row'];

   /** Re-export domain types for convenience */
   export type { Analysis, AnalysisMode, AnalysisStatus, NicheInterpreted } from './analysis';
   export type { Competitor, WebsiteData, SeoData, SocialData, MetaAdsData, GoogleAdsData, GmbData } from './competitor';
   export type { ViralContent, ContentPlatform, EngagementMetrics, HookBodyCta } from './viral';
   ```

5. Create `.env.example` (per D-19, FOUND-06):
   ```
   # Supabase
   NEXT_PUBLIC_SUPABASE_URL=url_do_projeto_supabase
   NEXT_PUBLIC_SUPABASE_ANON_KEY=chave_anonima_supabase
   SUPABASE_SERVICE_ROLE_KEY=chave_service_role_supabase

   # Apify
   APIFY_API_TOKEN=token_da_apify

   # Gemini AI
   GEMINI_API_KEY=chave_da_gemini_api

   # Transcricao (Assembly AI)
   ASSEMBLY_AI_API_KEY=chave_assembly_ai

   # Trigger.dev
   TRIGGER_SECRET_KEY=chave_secreta_trigger_dev

   # Bunny CDN + Storage
   BUNNY_STORAGE_API_KEY=chave_api_bunny_storage
   BUNNY_STORAGE_ZONE_NAME=nome_da_storage_zone
   BUNNY_CDN_URL=url_do_pullzone_bunny

   # App
   NEXT_PUBLIC_APP_URL=http://localhost:3000
   ```

6. Create `src/config/supabase.ts` (per D-19, Pattern 1 from RESEARCH):
   ```typescript
   import { z } from 'zod';

   const supabaseConfigSchema = z.object({
     url: z.string().url().min(1),
     anonKey: z.string().min(1),
     serviceRoleKey: z.string().min(1),
   });

   /** Configuracao do Supabase validada via Zod. Falha no import se variaveis ausentes. */
   export const supabaseConfig = supabaseConfigSchema.parse({
     url: process.env.NEXT_PUBLIC_SUPABASE_URL,
     anonKey: process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,
     serviceRoleKey: process.env.SUPABASE_SERVICE_ROLE_KEY,
   });

   export type SupabaseConfig = z.infer<typeof supabaseConfigSchema>;
   ```

7. Create `src/config/gemini.ts`:
   ```typescript
   import { z } from 'zod';

   const geminiConfigSchema = z.object({
     apiKey: z.string().min(1),
     model: z.string().default('gemini-2.0-flash'),
   });

   /** Configuracao da Gemini API validada via Zod */
   export const geminiConfig = geminiConfigSchema.parse({
     apiKey: process.env.GEMINI_API_KEY,
     model: 'gemini-2.0-flash',
   });

   export type GeminiConfig = z.infer<typeof geminiConfigSchema>;
   ```

8. Create `src/config/apify.ts`:
   ```typescript
   import { z } from 'zod';

   const apifyConfigSchema = z.object({
     apiToken: z.string().min(1),
   });

   /** Configuracao da Apify validada via Zod */
   export const apifyConfig = apifyConfigSchema.parse({
     apiToken: process.env.APIFY_API_TOKEN,
   });

   /** IDs dos actors da Apify usados no projeto */
   export const APIFY_ACTORS = {
     instagram: 'apify/instagram-scraper',
     tiktok: 'clockworks/tiktok-scraper',
     facebookAds: 'apify/facebook-ads-scraper',
     similarweb: 'tri_angle/similarweb-scraper',
     googleMaps: 'compass/google-maps-scraper',
     googleAds: 'apify/google-ads-scraper',
     website: 'apify/website-content-crawler',
   } as const;

   export type ApifyConfig = z.infer<typeof apifyConfigSchema>;
   ```

9. Create `src/config/bunny.ts`:
   ```typescript
   import { z } from 'zod';

   const bunnyConfigSchema = z.object({
     storageApiKey: z.string().min(1),
     storageZoneName: z.string().min(1),
     cdnUrl: z.string().url().min(1),
   });

   /** Configuracao do Bunny CDN + Storage validada via Zod */
   export const bunnyConfig = bunnyConfigSchema.parse({
     storageApiKey: process.env.BUNNY_STORAGE_API_KEY,
     storageZoneName: process.env.BUNNY_STORAGE_ZONE_NAME,
     cdnUrl: process.env.BUNNY_CDN_URL,
   });

   export type BunnyConfig = z.infer<typeof bunnyConfigSchema>;
   ```

10. Create `src/config/assemblyai.ts`:
    ```typescript
    import { z } from 'zod';

    const assemblyaiConfigSchema = z.object({
      apiKey: z.string().min(1),
    });

    /** Configuracao do AssemblyAI validada via Zod */
    export const assemblyaiConfig = assemblyaiConfigSchema.parse({
      apiKey: process.env.ASSEMBLY_AI_API_KEY,
    });

    export type AssemblyAIConfig = z.infer<typeof assemblyaiConfigSchema>;
    ```

11. Create `src/config/trigger.ts`:
    ```typescript
    import { z } from 'zod';

    const triggerConfigSchema = z.object({
      secretKey: z.string().min(1),
    });

    /** Configuracao do Trigger.dev validada via Zod */
    export const triggerConfig = triggerConfigSchema.parse({
      secretKey: process.env.TRIGGER_SECRET_KEY,
    });

    export type TriggerConfig = z.infer<typeof triggerConfigSchema>;
    ```

12. Create `src/utils/validators.ts` with Zod schemas for input validation:
    ```typescript
    import { z } from 'zod';

    /** Schema de validacao do input de analise */
    export const analysisInputSchema = z.object({
      nicheInput: z.string()
        .min(3, 'Descricao do nicho deve ter pelo menos 3 caracteres')
        .max(500, 'Descricao do nicho deve ter no maximo 500 caracteres'),
      mode: z.enum(['quick', 'complete']).default('quick'),
      userBusinessUrl: z.string().url('URL do negocio invalida').nullable().optional(),
    });

    /** Schema de validacao da interpretacao do nicho */
    export const nicheInterpretedSchema = z.object({
      niche: z.string().min(1),
      segment: z.string().min(1),
      region: z.string().min(1),
    });

    export type AnalysisInputValidated = z.infer<typeof analysisInputSchema>;
    export type NicheInterpretedValidated = z.infer<typeof nicheInterpretedSchema>;
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/types/analysis.ts contains `export type AnalysisMode =` and `export interface Analysis {`
    - src/types/analysis.ts contains `export interface NicheInterpreted {`
    - src/types/competitor.ts contains `export interface Competitor {` and `export interface WebsiteData {`
    - src/types/competitor.ts contains `export interface SeoData {` and `export interface SocialData {`
    - src/types/competitor.ts contains `export interface MetaAdsData {` and `export interface GoogleAdsData {` and `export interface GmbData {`
    - src/types/viral.ts contains `export type ContentPlatform =` and `export interface ViralContent {`
    - src/types/viral.ts contains `export interface HookBodyCta {` and `export interface EngagementMetrics {`
    - src/types/database.ts contains `export interface Database {` and `export type Tables<`
    - src/types/database.ts contains `export interface Synthesis {` and `export interface Recommendation {` and `export interface CreativeScript {`
    - src/config/supabase.ts contains `z.object(` and `supabaseConfig`
    - src/config/gemini.ts contains `gemini-2.0-flash` per D-16
    - src/config/apify.ts contains `APIFY_ACTORS` with all actor IDs
    - src/config/bunny.ts contains `bunnyConfig` and `z.object(`
    - src/config/assemblyai.ts contains `assemblyaiConfig` and `z.object(`
    - src/config/trigger.ts contains `triggerConfig` and `z.object(`
    - src/utils/validators.ts contains `analysisInputSchema` and `nicheInterpretedSchema`
    - .env.example contains `NEXT_PUBLIC_SUPABASE_URL`, `GEMINI_API_KEY`, `APIFY_API_TOKEN`, `ASSEMBLY_AI_API_KEY`, `TRIGGER_SECRET_KEY`, `BUNNY_STORAGE_API_KEY`, `BUNNY_CDN_URL`, `NEXT_PUBLIC_APP_URL`
    - `npx tsc --noEmit` exits with code 0 (no type errors)
  </acceptance_criteria>
  <done>All 4 TypeScript type files exist with complete domain model, all 6 config files exist with Zod validation, .env.example has all 14 environment variables, and TypeScript compilation passes with no errors.</done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. `npx tsc --noEmit` passes with zero errors
3. All type files importable: `import type { Analysis } from '@/types/analysis'`
4. .env.example contains all required variables
5. No `tailwind.config.js` or `tailwind.config.ts` file exists
6. Package.json has Zod override and all correct dependencies
</verification>

<success_criteria>
- Next.js dev server starts and shows the LupAI placeholder page
- TypeScript compilation succeeds with zero errors
- All domain types (Analysis, Competitor, ViralContent, Synthesis, Database) are defined and exportable
- All config files validate env vars with Zod
- .env.example documents all 14 environment variables
- Tailwind v4 CSS-first config active (no JS config file)
- Vitest, ESLint, and TypeScript configured and runnable
- Complete folder structure matches CLAUDE.md specification
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-project-setup/01-01-SUMMARY.md`
</output>
