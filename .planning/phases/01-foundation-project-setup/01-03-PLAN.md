---
phase: 01-foundation-project-setup
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/fixtures/instagram.json
  - tests/fixtures/tiktok.json
  - tests/fixtures/facebook-ads.json
  - tests/fixtures/similarweb.json
  - tests/fixtures/google-ads.json
  - tests/fixtures/website.json
  - tests/fixtures/google-maps.json
  - tests/fixtures/gemini-understand.json
  - tests/fixtures/gemini-synthesis.json
  - tests/fixtures/gemini-creative.json
  - tests/fixtures/assemblyai-transcription.json
  - tests/fixtures/factories.ts
  - tests/unit/types.test.ts
  - tests/unit/validators.test.ts
autonomous: true
requirements:
  - FOUND-05

must_haves:
  truths:
    - "Fixture JSON files exist for every Apify actor output type (filtered format, not raw)"
    - "Fixture JSON files exist for Gemini AI responses (understand, synthesis, creative)"
    - "Fixture JSON file exists for AssemblyAI transcription response"
    - "Factory functions create typed test data with overridable defaults"
    - "Tests pass confirming types and validators work correctly"
  artifacts:
    - path: "tests/fixtures/factories.ts"
      provides: "Factory functions for all domain entities"
      exports: ["createAnalysis", "createCompetitor", "createViralContent", "createSynthesis"]
    - path: "tests/fixtures/instagram.json"
      provides: "Sample filtered Instagram scraper output"
      min_lines: 5
    - path: "tests/fixtures/gemini-understand.json"
      provides: "Sample Gemini niche understanding response"
      contains: "niche"
    - path: "tests/unit/types.test.ts"
      provides: "Type validation tests"
      min_lines: 20
    - path: "tests/unit/validators.test.ts"
      provides: "Input validator tests"
      min_lines: 20
  key_links:
    - from: "tests/fixtures/factories.ts"
      to: "src/types/analysis.ts"
      via: "Import types for factory return values"
      pattern: "import.*from.*@/types/analysis"
    - from: "tests/unit/validators.test.ts"
      to: "src/utils/validators.ts"
      via: "Tests validate Zod schemas"
      pattern: "import.*from.*@/utils/validators"
---

<objective>
Create fixture/mock infrastructure for development and testing without burning API credits, plus initial tests to verify the type system works.

Purpose: CLAUDE.md mandates no API credit burning during development. Fixtures let all subsequent phases develop and test with realistic data shapes. Factory functions prevent test boilerplate.
Output: 11 JSON fixture files (7 Apify + 3 Gemini + 1 AssemblyAI), factory functions, and passing tests.
</objective>

<execution_context>
@C:/Users/biela/OneDrive/Documentos/github/lupai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/biela/OneDrive/Documentos/github/lupai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-project-setup/01-CONTEXT.md
@.planning/phases/01-foundation-project-setup/01-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Types from Plan 01 that fixtures must match -->

From src/types/analysis.ts:
```typescript
export type AnalysisMode = 'quick' | 'complete';
export type AnalysisStatus = 'pending' | 'processing' | 'completed' | 'failed';
export interface NicheInterpreted { niche: string; segment: string; region: string; }
export interface Analysis { id: string; nicheInput: string; nicheInterpreted: NicheInterpreted | null; mode: AnalysisMode; status: AnalysisStatus; userBusinessUrl: string | null; triggerRunId: string | null; createdAt: string; updatedAt: string; }
```

From src/types/competitor.ts:
```typescript
export interface WebsiteData { positioning: string | null; offer: string | null; pricing: string | null; metaTags: { title: string | null; description: string | null; keywords: string[] } }
export interface SeoData { estimatedAuthority: number | null; topKeywords: string[]; estimatedTraffic: number | null; backlinks: number | null; }
export interface SocialData { instagram: { followers: number | null; postingFrequency: string | null; engagementRate: number | null; topPosts: SocialPost[] } | null; tiktok: { ... } | null; }
export interface MetaAdsData { activeAdsCount: number; ads: MetaAd[] }
export interface GoogleAdsData { hasSearchAds: boolean; paidKeywords: string[]; estimatedBudget: string | null; }
export interface GmbData { name: string | null; rating: number | null; reviewCount: number | null; address: string | null; phone: string | null; categories: string[] }
export interface Competitor { id: string; analysisId: string; name: string; websiteUrl: string | null; ... }
```

From src/types/viral.ts:
```typescript
export type ContentPlatform = 'tiktok' | 'instagram' | 'facebook';
export interface EngagementMetrics { views: number | null; likes: number; comments: number; shares: number | null; saves: number | null; }
export interface HookBodyCta { hook: string; body: string; cta: string; hookDurationSeconds: number | null; totalDurationSeconds: number | null; }
export interface ViralContent { id: string; analysisId: string; platform: ContentPlatform; sourceUrl: string; bunnyUrl: string | null; transcription: string | null; hookBodyCta: HookBodyCta | null; engagementMetrics: EngagementMetrics; createdAt: string; }
```

From src/types/database.ts:
```typescript
export interface Recommendation { title: string; description: string; priority: 'high' | 'medium' | 'low'; category: string; }
export interface CreativeScript { title: string; hook: string; body: string; cta: string; format: string; estimatedDurationSeconds: number; platform: string; }
export interface Synthesis { id: string; analysisId: string; strategicOverview: string; recommendations: Recommendation[]; creativeScripts: CreativeScript[]; comparativeAnalysis: ComparativeAnalysis | null; createdAt: string; }
```

From src/utils/validators.ts:
```typescript
export const analysisInputSchema: z.ZodObject<...>;
export const nicheInterpretedSchema: z.ZodObject<...>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all fixture JSON files</name>
  <files>tests/fixtures/instagram.json, tests/fixtures/tiktok.json, tests/fixtures/facebook-ads.json, tests/fixtures/similarweb.json, tests/fixtures/google-ads.json, tests/fixtures/website.json, tests/fixtures/google-maps.json, tests/fixtures/gemini-understand.json, tests/fixtures/gemini-synthesis.json, tests/fixtures/gemini-creative.json, tests/fixtures/assemblyai-transcription.json</files>
  <read_first>src/types/competitor.ts, src/types/viral.ts, src/types/analysis.ts, src/types/database.ts</read_first>
  <action>
Create the `tests/fixtures/` directory and all 11 JSON fixture files. Per D-22, fixtures must match the **filtered** output format (what gets stored in DB JSONB columns), NOT raw Apify actor output. Use realistic PT-BR data about "clinicas odontologicas em SP" as the example niche.

1. **tests/fixtures/instagram.json** — matches SocialData['instagram'] shape:
   ```json
   {
     "followers": 15200,
     "postingFrequency": "4x por semana",
     "engagementRate": 3.2,
     "topPosts": [
       {
         "url": "https://instagram.com/p/abc123",
         "caption": "Sorriso perfeito comeca com uma boa consulta! Agende ja.",
         "likes": 342,
         "comments": 28,
         "shares": null,
         "postedAt": "2026-03-20T14:00:00Z"
       },
       {
         "url": "https://instagram.com/p/def456",
         "caption": "Antes e depois: clareamento dental em 1 sessao",
         "likes": 891,
         "comments": 67,
         "shares": null,
         "postedAt": "2026-03-18T10:30:00Z"
       },
       {
         "url": "https://instagram.com/p/ghi789",
         "caption": "Dicas de higiene bucal que voce precisa saber",
         "likes": 256,
         "comments": 15,
         "shares": null,
         "postedAt": "2026-03-15T16:00:00Z"
       }
     ]
   }
   ```

2. **tests/fixtures/tiktok.json** — matches SocialData['tiktok'] shape:
   ```json
   {
     "followers": 8400,
     "postingFrequency": "3x por semana",
     "engagementRate": 5.1,
     "topPosts": [
       {
         "url": "https://tiktok.com/@clinica/video/123",
         "caption": "POV: voce descobrindo que clareamento nao doi",
         "likes": 2340,
         "comments": 156,
         "shares": 89,
         "postedAt": "2026-03-22T09:00:00Z"
       },
       {
         "url": "https://tiktok.com/@clinica/video/456",
         "caption": "3 sinais de que voce precisa de um canal",
         "likes": 1567,
         "comments": 98,
         "shares": 45,
         "postedAt": "2026-03-19T11:00:00Z"
       }
     ]
   }
   ```

3. **tests/fixtures/facebook-ads.json** — matches MetaAdsData shape:
   ```json
   {
     "activeAdsCount": 3,
     "ads": [
       {
         "adId": "meta-ad-001",
         "creativeUrl": "https://example.com/creative1.jpg",
         "copyText": "Clareamento dental com 40% de desconto! Agende sua avaliacao gratuita.",
         "format": "image",
         "startedAt": "2026-03-01T00:00:00Z",
         "isActive": true
       },
       {
         "adId": "meta-ad-002",
         "creativeUrl": "https://example.com/creative2.mp4",
         "copyText": "Implante dentario em ate 12x sem juros. Consulta gratis!",
         "format": "video",
         "startedAt": "2026-02-15T00:00:00Z",
         "isActive": true
       },
       {
         "adId": "meta-ad-003",
         "creativeUrl": "https://example.com/creative3.jpg",
         "copyText": "Aparelho invisivel: seu sorriso perfeito sem metal",
         "format": "carousel",
         "startedAt": "2026-03-10T00:00:00Z",
         "isActive": true
       }
     ]
   }
   ```

4. **tests/fixtures/similarweb.json** — matches SeoData shape:
   ```json
   {
     "estimatedAuthority": 35,
     "topKeywords": [
       "clinica odontologica sp",
       "dentista zona sul sp",
       "clareamento dental preco",
       "implante dentario sp",
       "aparelho invisivel"
     ],
     "estimatedTraffic": 12000,
     "backlinks": 450
   }
   ```

5. **tests/fixtures/google-ads.json** — matches GoogleAdsData shape:
   ```json
   {
     "hasSearchAds": true,
     "paidKeywords": [
       "dentista sp",
       "clinica odontologica",
       "implante dentario preco",
       "clareamento dental"
     ],
     "estimatedBudget": "R$ 2.000-5.000/mes"
   }
   ```

6. **tests/fixtures/website.json** — matches WebsiteData shape:
   ```json
   {
     "positioning": "Clinica odontologica premium com foco em estetica dental e implantes",
     "offer": "Avaliacao gratuita, clareamento com desconto, parcelamento em 12x",
     "pricing": "Clareamento a partir de R$ 800, Implante a partir de R$ 3.500",
     "metaTags": {
       "title": "Clinica Sorriso SP | Dentista em Sao Paulo",
       "description": "Clinica odontologica em SP com especialistas em estetica dental, implantes e ortodontia. Agende sua consulta gratuita.",
       "keywords": ["dentista sp", "clinica odontologica", "implante dentario", "clareamento dental"]
     }
   }
   ```

7. **tests/fixtures/google-maps.json** — matches GmbData shape:
   ```json
   {
     "name": "Clinica Sorriso SP",
     "rating": 4.7,
     "reviewCount": 234,
     "address": "Av. Paulista, 1000 - Bela Vista, Sao Paulo - SP",
     "phone": "(11) 3456-7890",
     "categories": ["Dentista", "Clinica odontologica", "Ortodontista"]
   }
   ```

8. **tests/fixtures/gemini-understand.json** — matches NicheInterpreted shape (per D-23):
   ```json
   {
     "niche": "odontologia",
     "segment": "clinicas odontologicas",
     "region": "Sao Paulo, SP"
   }
   ```

9. **tests/fixtures/gemini-synthesis.json** — matches synthesis output shape (per D-23):
   ```json
   {
     "strategicOverview": "O mercado de clinicas odontologicas em SP e altamente competitivo, com forte presenca digital. Os principais concorrentes investem em Meta Ads com foco em clareamento e implantes. A taxa de engajamento no Instagram e superior a media do setor (3.2%), indicando que conteudo visual de antes/depois tem alto apelo.",
     "recommendations": [
       {
         "title": "Investir em conteudo de antes/depois no Instagram",
         "description": "Clinica Sorriso SP gera 891 likes por post de antes/depois vs 256 em posts educativos. Crie serie semanal mostrando resultados reais de clareamento e implantes.",
         "priority": "high",
         "category": "conteudo"
       },
       {
         "title": "Comecar presenca no TikTok",
         "description": "Clinica Sorriso tem 8.400 seguidores no TikTok com engajamento de 5.1%. O formato POV e dicas rapidas funciona bem. Comece com 3 videos por semana.",
         "priority": "high",
         "category": "redes-sociais"
       },
       {
         "title": "Otimizar keywords de Google Ads",
         "description": "Concorrente investe em 'dentista sp' e 'implante dentario preco'. Adicione keywords de cauda longa como 'clareamento dental zona sul sp' para CPC menor.",
         "priority": "medium",
         "category": "anuncios"
       }
     ]
   }
   ```

10. **tests/fixtures/gemini-creative.json** — matches CreativeScript[] shape (per D-23):
    ```json
    [
      {
        "title": "POV: Primeira consulta na clinica",
        "hook": "Voce sabia que 70% das pessoas tem medo de dentista? Olha o que acontece quando elas chegam aqui...",
        "body": "Mostra a recepcao acolhedora, o ambiente moderno, o dentista explicando o procedimento com calma. Corta para o paciente sorrindo no final.",
        "cta": "Agende sua avaliacao gratuita pelo link na bio. Seu sorriso merece!",
        "format": "Reels/TikTok",
        "estimatedDurationSeconds": 30,
        "platform": "Instagram"
      },
      {
        "title": "3 sinais de que voce precisa de um clareamento",
        "hook": "Se voce percebe ISSO no espelho, precisa assistir ate o final",
        "body": "Sinal 1: dentes amarelados mesmo escovando bem. Sinal 2: sorriso 'apagado' nas fotos. Sinal 3: voce evita sorrir abertamente. A boa noticia: clareamento resolve em 1 sessao.",
        "cta": "Quer saber o preco? Comenta 'EU QUERO' que mando no direct!",
        "format": "Reels/TikTok",
        "estimatedDurationSeconds": 45,
        "platform": "TikTok"
      },
      {
        "title": "Antes e depois REAL de implante",
        "hook": "Ela perdeu o dente da frente num acidente. Olha como ficou depois...",
        "body": "Mostra foto/video do antes com depoimento do paciente. Processo simplificado do implante. Reveal do resultado final com reacao genuina do paciente.",
        "cta": "Implante a partir de R$ 3.500 em ate 12x. Link na bio pra agendar.",
        "format": "Reels",
        "estimatedDurationSeconds": 60,
        "platform": "Instagram"
      }
    ]
    ```

11. **tests/fixtures/assemblyai-transcription.json** — matches TranscriptionResult shape (per D-24):
    ```json
    {
      "text": "Voce sabia que setenta porcento das pessoas tem medo de dentista? Pois e, mas olha o que acontece quando elas chegam aqui na nossa clinica. A gente recebe com cafe, um ambiente super acolhedor, e o dentista explica tudo antes de comecar. No final, todo mundo sai sorrindo. Literalmente. Agende sua avaliacao gratuita pelo link na bio.",
      "durationSeconds": 32,
      "language": "pt"
    }
    ```
  </action>
  <verify>
    <automated>ls tests/fixtures/*.json | wc -l | grep -q "11" &amp;&amp; echo "PASS: 11 fixture files" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/instagram.json contains `"followers"` and `"topPosts"` and `"engagementRate"`
    - tests/fixtures/tiktok.json contains `"followers"` and `"topPosts"`
    - tests/fixtures/facebook-ads.json contains `"activeAdsCount"` and `"ads"` array with `"adId"`
    - tests/fixtures/similarweb.json contains `"estimatedAuthority"` and `"topKeywords"`
    - tests/fixtures/google-ads.json contains `"hasSearchAds"` and `"paidKeywords"`
    - tests/fixtures/website.json contains `"positioning"` and `"metaTags"`
    - tests/fixtures/google-maps.json contains `"rating"` and `"reviewCount"` and `"categories"`
    - tests/fixtures/gemini-understand.json contains `"niche"` and `"segment"` and `"region"`
    - tests/fixtures/gemini-synthesis.json contains `"strategicOverview"` and `"recommendations"` array
    - tests/fixtures/gemini-creative.json contains `"hook"` and `"body"` and `"cta"` and `"format"`
    - tests/fixtures/assemblyai-transcription.json contains `"text"` and `"durationSeconds"` and `"language"`
    - All fixture data is in PT-BR (Portuguese content, not English)
    - Exactly 11 JSON files exist in tests/fixtures/
  </acceptance_criteria>
  <done>All 11 fixture JSON files exist with realistic PT-BR data matching the filtered output format stored in DB (not raw Apify output). Coverage: 7 Apify actors + 3 Gemini responses + 1 AssemblyAI transcription.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create factory functions and tests</name>
  <files>tests/fixtures/factories.ts, tests/unit/types.test.ts, tests/unit/validators.test.ts</files>
  <read_first>src/types/analysis.ts, src/types/competitor.ts, src/types/viral.ts, src/types/database.ts, src/utils/validators.ts, vitest.config.ts</read_first>
  <behavior>
    - createAnalysis() returns a valid Analysis object with all required fields
    - createAnalysis({ status: 'failed' }) returns Analysis with status overridden to 'failed'
    - createCompetitor() returns a valid Competitor object with all required fields
    - createViralContent() returns a valid ViralContent object with all required fields
    - createSynthesis() returns a valid Synthesis object with recommendations and scripts
    - analysisInputSchema.parse({ nicheInput: 'clinicas em SP', mode: 'quick' }) succeeds
    - analysisInputSchema.parse({ nicheInput: 'ab' }) throws (min 3 chars)
    - analysisInputSchema.parse({ nicheInput: '' }) throws (min 3 chars)
    - nicheInterpretedSchema.parse({ niche: 'x', segment: 'y', region: 'z' }) succeeds
    - nicheInterpretedSchema.parse({ niche: '' }) throws (min 1 char)
  </behavior>
  <action>
1. Create `tests/fixtures/factories.ts` (per D-21, Pattern 4 from RESEARCH):
   ```typescript
   import type { Analysis } from '@/types/analysis';
   import type { Competitor } from '@/types/competitor';
   import type { ViralContent } from '@/types/viral';
   import type { Synthesis, Recommendation, CreativeScript } from '@/types/database';

   /** Cria dados de analise com valores padrao para testes */
   export const createAnalysis = (overrides?: Partial<Analysis>): Analysis => ({
     id: 'test-analysis-001',
     nicheInput: 'clinicas odontologicas em SP',
     nicheInterpreted: {
       niche: 'odontologia',
       segment: 'clinicas odontologicas',
       region: 'Sao Paulo, SP',
     },
     mode: 'quick',
     status: 'completed',
     userBusinessUrl: null,
     triggerRunId: null,
     createdAt: '2026-03-27T12:00:00Z',
     updatedAt: '2026-03-27T12:05:00Z',
     ...overrides,
   });

   /** Cria dados de concorrente com valores padrao para testes */
   export const createCompetitor = (overrides?: Partial<Competitor>): Competitor => ({
     id: 'test-competitor-001',
     analysisId: 'test-analysis-001',
     name: 'Clinica Sorriso SP',
     websiteUrl: 'https://clinicasorriso.com.br',
     websiteData: null,
     seoData: null,
     socialData: null,
     metaAdsData: null,
     googleAdsData: null,
     gmbData: null,
     createdAt: '2026-03-27T12:01:00Z',
     ...overrides,
   });

   /** Cria dados de conteudo viral com valores padrao para testes */
   export const createViralContent = (overrides?: Partial<ViralContent>): ViralContent => ({
     id: 'test-viral-001',
     analysisId: 'test-analysis-001',
     platform: 'instagram',
     sourceUrl: 'https://instagram.com/reel/abc123',
     bunnyUrl: null,
     transcription: null,
     hookBodyCta: null,
     engagementMetrics: {
       views: 15000,
       likes: 1200,
       comments: 89,
       shares: 45,
       saves: 120,
     },
     createdAt: '2026-03-27T12:02:00Z',
     ...overrides,
   });

   /** Cria dados de recomendacao com valores padrao */
   export const createRecommendation = (overrides?: Partial<Recommendation>): Recommendation => ({
     title: 'Investir em conteudo de antes/depois',
     description: 'Posts de antes/depois geram 3x mais engajamento que posts educativos.',
     priority: 'high',
     category: 'conteudo',
     ...overrides,
   });

   /** Cria dados de roteiro criativo com valores padrao */
   export const createCreativeScript = (overrides?: Partial<CreativeScript>): CreativeScript => ({
     title: 'POV: Primeira consulta',
     hook: 'Voce sabia que 70% das pessoas tem medo de dentista?',
     body: 'Mostra a recepcao acolhedora e o ambiente moderno da clinica.',
     cta: 'Agende sua avaliacao gratuita pelo link na bio!',
     format: 'Reels/TikTok',
     estimatedDurationSeconds: 30,
     platform: 'Instagram',
     ...overrides,
   });

   /** Cria dados de sintese com valores padrao para testes */
   export const createSynthesis = (overrides?: Partial<Synthesis>): Synthesis => ({
     id: 'test-synthesis-001',
     analysisId: 'test-analysis-001',
     strategicOverview: 'O mercado de clinicas odontologicas em SP e altamente competitivo.',
     recommendations: [createRecommendation()],
     creativeScripts: [createCreativeScript()],
     comparativeAnalysis: null,
     createdAt: '2026-03-27T12:05:00Z',
     ...overrides,
   });
   ```

2. Create `tests/unit/types.test.ts`:
   ```typescript
   import { describe, it, expect } from 'vitest';
   import {
     createAnalysis,
     createCompetitor,
     createViralContent,
     createSynthesis,
     createRecommendation,
     createCreativeScript,
   } from '../fixtures/factories';

   describe('Factory: createAnalysis', () => {
     it('retorna analise com todos os campos obrigatorios', () => {
       const analysis = createAnalysis();
       expect(analysis.id).toBe('test-analysis-001');
       expect(analysis.nicheInput).toBe('clinicas odontologicas em SP');
       expect(analysis.mode).toBe('quick');
       expect(analysis.status).toBe('completed');
       expect(analysis.nicheInterpreted).not.toBeNull();
       expect(analysis.nicheInterpreted?.niche).toBe('odontologia');
     });

     it('permite sobrescrever campos individuais', () => {
       const analysis = createAnalysis({ status: 'failed', mode: 'complete' });
       expect(analysis.status).toBe('failed');
       expect(analysis.mode).toBe('complete');
       expect(analysis.nicheInput).toBe('clinicas odontologicas em SP');
     });

     it('permite nicheInterpreted nulo', () => {
       const analysis = createAnalysis({ nicheInterpreted: null });
       expect(analysis.nicheInterpreted).toBeNull();
     });
   });

   describe('Factory: createCompetitor', () => {
     it('retorna concorrente com todos os campos obrigatorios', () => {
       const competitor = createCompetitor();
       expect(competitor.id).toBe('test-competitor-001');
       expect(competitor.name).toBe('Clinica Sorriso SP');
       expect(competitor.analysisId).toBe('test-analysis-001');
       expect(competitor.websiteUrl).toBe('https://clinicasorriso.com.br');
     });

     it('permite sobrescrever nome e website', () => {
       const competitor = createCompetitor({
         name: 'Dental Plus',
         websiteUrl: 'https://dentalplus.com.br',
       });
       expect(competitor.name).toBe('Dental Plus');
       expect(competitor.websiteUrl).toBe('https://dentalplus.com.br');
     });

     it('campos JSONB iniciam como null', () => {
       const competitor = createCompetitor();
       expect(competitor.websiteData).toBeNull();
       expect(competitor.seoData).toBeNull();
       expect(competitor.socialData).toBeNull();
       expect(competitor.metaAdsData).toBeNull();
       expect(competitor.googleAdsData).toBeNull();
       expect(competitor.gmbData).toBeNull();
     });
   });

   describe('Factory: createViralContent', () => {
     it('retorna conteudo viral com metricas de engajamento', () => {
       const viral = createViralContent();
       expect(viral.platform).toBe('instagram');
       expect(viral.engagementMetrics.likes).toBe(1200);
       expect(viral.engagementMetrics.views).toBe(15000);
     });

     it('permite sobrescrever plataforma', () => {
       const viral = createViralContent({ platform: 'tiktok' });
       expect(viral.platform).toBe('tiktok');
     });
   });

   describe('Factory: createSynthesis', () => {
     it('retorna sintese com recomendacoes e roteiros', () => {
       const synthesis = createSynthesis();
       expect(synthesis.recommendations.length).toBeGreaterThan(0);
       expect(synthesis.creativeScripts.length).toBeGreaterThan(0);
       expect(synthesis.strategicOverview).toContain('odontologicas');
     });

     it('comparative analysis e null por padrao', () => {
       const synthesis = createSynthesis();
       expect(synthesis.comparativeAnalysis).toBeNull();
     });
   });

   describe('Factory: createRecommendation', () => {
     it('retorna recomendacao com prioridade alta por padrao', () => {
       const rec = createRecommendation();
       expect(rec.priority).toBe('high');
       expect(rec.title).toBeTruthy();
       expect(rec.description).toBeTruthy();
     });
   });

   describe('Factory: createCreativeScript', () => {
     it('retorna roteiro com hook, corpo e CTA', () => {
       const script = createCreativeScript();
       expect(script.hook).toBeTruthy();
       expect(script.body).toBeTruthy();
       expect(script.cta).toBeTruthy();
       expect(script.estimatedDurationSeconds).toBeGreaterThan(0);
     });
   });
   ```

3. Create `tests/unit/validators.test.ts`:
   ```typescript
   import { describe, it, expect } from 'vitest';
   import { analysisInputSchema, nicheInterpretedSchema } from '@/utils/validators';

   describe('analysisInputSchema', () => {
     it('aceita input valido com modo quick', () => {
       const result = analysisInputSchema.parse({
         nicheInput: 'clinicas odontologicas em SP',
         mode: 'quick',
       });
       expect(result.nicheInput).toBe('clinicas odontologicas em SP');
       expect(result.mode).toBe('quick');
     });

     it('aceita input valido com modo complete e URL', () => {
       const result = analysisInputSchema.parse({
         nicheInput: 'academias de crossfit',
         mode: 'complete',
         userBusinessUrl: 'https://minhacademia.com.br',
       });
       expect(result.mode).toBe('complete');
       expect(result.userBusinessUrl).toBe('https://minhacademia.com.br');
     });

     it('usa modo quick como padrao', () => {
       const result = analysisInputSchema.parse({
         nicheInput: 'restaurantes japoneses',
       });
       expect(result.mode).toBe('quick');
     });

     it('rejeita nicheInput muito curto (menos de 3 caracteres)', () => {
       expect(() => analysisInputSchema.parse({ nicheInput: 'ab' })).toThrow();
     });

     it('rejeita nicheInput vazio', () => {
       expect(() => analysisInputSchema.parse({ nicheInput: '' })).toThrow();
     });

     it('rejeita nicheInput muito longo (mais de 500 caracteres)', () => {
       const longInput = 'a'.repeat(501);
       expect(() => analysisInputSchema.parse({ nicheInput: longInput })).toThrow();
     });

     it('rejeita URL invalida', () => {
       expect(() =>
         analysisInputSchema.parse({
           nicheInput: 'teste',
           userBusinessUrl: 'nao-e-uma-url',
         })
       ).toThrow();
     });

     it('aceita userBusinessUrl null', () => {
       const result = analysisInputSchema.parse({
         nicheInput: 'teste valido',
         userBusinessUrl: null,
       });
       expect(result.userBusinessUrl).toBeNull();
     });
   });

   describe('nicheInterpretedSchema', () => {
     it('aceita interpretacao valida', () => {
       const result = nicheInterpretedSchema.parse({
         niche: 'odontologia',
         segment: 'clinicas',
         region: 'Sao Paulo, SP',
       });
       expect(result.niche).toBe('odontologia');
       expect(result.segment).toBe('clinicas');
       expect(result.region).toBe('Sao Paulo, SP');
     });

     it('rejeita niche vazio', () => {
       expect(() =>
         nicheInterpretedSchema.parse({
           niche: '',
           segment: 'clinicas',
           region: 'SP',
         })
       ).toThrow();
     });

     it('rejeita objeto incompleto (sem region)', () => {
       expect(() =>
         nicheInterpretedSchema.parse({
           niche: 'odontologia',
           segment: 'clinicas',
         })
       ).toThrow();
     });
   });
   ```
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/factories.ts contains `export const createAnalysis`, `export const createCompetitor`, `export const createViralContent`, `export const createSynthesis`, `export const createRecommendation`, `export const createCreativeScript`
    - tests/fixtures/factories.ts imports from `@/types/analysis`, `@/types/competitor`, `@/types/viral`, `@/types/database`
    - Each factory function accepts `Partial<T>` for overrides
    - tests/unit/types.test.ts contains at least 10 `it(` test cases
    - tests/unit/validators.test.ts contains at least 9 `it(` test cases
    - tests/unit/validators.test.ts imports `analysisInputSchema` and `nicheInterpretedSchema` from `@/utils/validators`
    - `npx vitest run` exits with code 0 (all tests pass)
  </acceptance_criteria>
  <done>Factory functions exist for all domain entities with overridable defaults. Tests verify factory output shapes, validator acceptance of valid data, and validator rejection of invalid data. All tests pass.</done>
</task>

</tasks>

<verification>
1. `ls tests/fixtures/*.json | wc -l` returns 11
2. All JSON fixtures contain PT-BR content
3. `npx vitest run` passes all tests with zero failures
4. Factory functions are typed and match domain entity interfaces
</verification>

<success_criteria>
- 11 JSON fixture files exist with realistic PT-BR data matching filtered output shapes
- Factory functions for Analysis, Competitor, ViralContent, Synthesis, Recommendation, CreativeScript
- Tests verify factory defaults, override behavior, and validator acceptance/rejection
- All tests pass with `npx vitest run`
- No fixture contains raw Apify output format (all match DB storage format per D-22)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-project-setup/01-03-SUMMARY.md`
</output>
