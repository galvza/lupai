---
phase: 07-ai-synthesis-creative-modeling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/database.ts
  - src/lib/validation/synthesisSchemas.ts
  - src/lib/ai/prompts.ts
  - src/lib/ai/synthesize.ts
  - src/lib/ai/creative.ts
  - tests/fixtures/factories.ts
  - tests/fixtures/gemini-synthesis-v2.json
  - tests/fixtures/gemini-creative-v2.json
  - tests/unit/synthesize-ai.test.ts
  - tests/unit/creative-ai.test.ts
autonomous: true
requirements:
  - SYNTH-01
  - SYNTH-02
  - SYNTH-03
  - CRTV-01
  - CRTV-02
  - CRTV-03

must_haves:
  truths:
    - "synthesizeAnalysis returns structured sections (marketOverview, competitorAnalysis, gapsAndOpportunities, viralPatterns) with title/summary/metrics/tags/detailed_analysis per section"
    - "synthesizeAnalysis returns recommendations with action/reason/priority/effort/expected_impact fields, never generic"
    - "generateCreativeScripts returns 3-5 scripts with nested hook/body/cta objects, format, estimated_duration_seconds, tone, inspiration_source"
    - "Both functions use Zod + zodToJsonSchema + validateOrNull pattern (not raw JSON.parse)"
    - "When viral patterns unavailable, creative scripts fallback to competitor analysis + niche best practices"
    - "When assembled context exceeds Gemini token budget, truncateContextIfNeeded dynamically reduces viral content to ViralPatterns only (per D-29)"
  artifacts:
    - path: "src/lib/validation/synthesisSchemas.ts"
      provides: "Zod schemas for synthesis output and creative output"
      exports: ["synthesisOutputSchema", "creativeOutputSchema", "recommendationSchema", "synthesisSectionSchema", "creativeScriptSchema"]
    - path: "src/lib/ai/synthesize.ts"
      provides: "Upgraded synthesis function using Zod structured output with dynamic token budget truncation"
      exports: ["synthesizeAnalysis", "truncateContextIfNeeded"]
    - path: "src/lib/ai/creative.ts"
      provides: "Upgraded creative function using Zod structured output"
      exports: ["generateCreativeScripts"]
    - path: "src/types/database.ts"
      provides: "Updated Recommendation and CreativeScript interfaces per D-08 and D-12"
      contains: "action: string"
    - path: "tests/unit/synthesize-ai.test.ts"
      provides: "Tests for synthesis AI module"
    - path: "tests/unit/creative-ai.test.ts"
      provides: "Tests for creative AI module"
  key_links:
    - from: "src/lib/ai/synthesize.ts"
      to: "src/lib/validation/synthesisSchemas.ts"
      via: "import synthesisOutputSchema"
      pattern: "zodToJsonSchema\\(synthesisOutputSchema\\)"
    - from: "src/lib/ai/creative.ts"
      to: "src/lib/validation/synthesisSchemas.ts"
      via: "import creativeOutputSchema"
      pattern: "zodToJsonSchema\\(creativeOutputSchema\\)"
    - from: "src/lib/ai/synthesize.ts"
      to: "src/lib/validation/extractionSchemas.ts"
      via: "import validateOrNull"
      pattern: "validateOrNull\\(synthesisOutputSchema"
    - from: "src/lib/ai/synthesize.ts"
      to: "@google/genai"
      via: "genai.models.countTokens for dynamic truncation"
      pattern: "countTokens"
---

<objective>
Update types, create Zod validation schemas, upgrade the existing AI synthesis and creative stub functions to use the mature Gemini structured output pattern (Zod + zodToJsonSchema + validateOrNull), and add comprehensive test coverage.

Purpose: Establish the data contracts and AI module implementations that Plan 02's Trigger.dev task and orchestrator integration depend on.
Output: Updated types, new Zod schemas, fully upgraded synthesizeAnalysis and generateCreativeScripts functions, test fixtures, and passing unit tests.
</objective>

<execution_context>
@C:/Users/biela/OneDrive/Documentos/github/lupai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/biela/OneDrive/Documentos/github/lupai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-synthesis-creative-modeling/07-CONTEXT.md
@.planning/phases/07-ai-synthesis-creative-modeling/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/database.ts (CURRENT — will be modified):
```typescript
export interface Recommendation {
  title: string;
  description: string;
  priority: 'high' | 'medium' | 'low';
  category: string;
}

export interface CreativeScript {
  title: string;
  hook: string;
  body: string;
  cta: string;
  format: string;
  estimatedDurationSeconds: number;
  platform: string;
}

export interface Synthesis {
  id: string;
  analysisId: string;
  strategicOverview: string;
  recommendations: Recommendation[];
  creativeScripts: CreativeScript[];
  comparativeAnalysis: ComparativeAnalysis | null;
  createdAt: string;
}
```

From src/lib/validation/extractionSchemas.ts (pattern to follow):
```typescript
export const validateOrNull = <T>(schema: z.ZodType<T>, data: unknown): T | null;
```

From src/lib/ai/hbc-extraction.ts (Gemini structured output pattern):
```typescript
import { zodToJsonSchema } from 'zod-to-json-schema';
import { validateOrNull } from '@/lib/validation/extractionSchemas';
// ...
config: {
  responseMimeType: 'application/json',
  responseJsonSchema: zodToJsonSchema(hookBodyCtaSchema),
},
// ...
return validateOrNull(hookBodyCtaSchema, parsed);
```

From src/lib/ai/viral-patterns.ts (complex structured output pattern):
```typescript
export const detectViralPatterns = async (inputs: PatternDetectionInput[]): Promise<ViralPatterns | null>;
```

From src/types/competitor.ts:
```typescript
export interface Competitor {
  id: string; analysisId: string; name: string; websiteUrl: string | null;
  websiteData: WebsiteData | null; seoData: SeoData | null;
  socialData: SocialData | null; metaAdsData: MetaAdsData | null;
  googleAdsData: GoogleAdsData | null; gmbData: GmbData | null;
  createdAt: string;
}
```

From src/types/viral.ts:
```typescript
export interface ViralContent { id: string; analysisId: string; platform: ContentPlatform; sourceUrl: string; bunnyUrl: string | null; transcription: string | null; hookBodyCta: HookBodyCta | null; engagementMetrics: EngagementMetrics; caption: string | null; creatorHandle: string | null; durationSeconds: number | null; postDate: string | null; createdAt: string; }
export interface ViralPatterns { hookPatterns: HookPattern[]; bodyStructures: BodyStructure[]; ctaPatterns: CtaPattern[]; dominantTone: string; bestPerformingDuration: { averageSeconds: number; range: string }; recurringFormulas: RecurringFormula[]; totalVideosAnalyzed: number; analysisConfidence: 'high' | 'medium' | 'low'; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update types, create Zod schemas, update fixtures</name>
  <files>src/types/database.ts, src/lib/validation/synthesisSchemas.ts, tests/fixtures/factories.ts, tests/fixtures/gemini-synthesis-v2.json, tests/fixtures/gemini-creative-v2.json</files>

  <read_first>
    - src/types/database.ts
    - src/lib/validation/extractionSchemas.ts
    - tests/fixtures/factories.ts
    - tests/fixtures/gemini-synthesis.json
    - tests/fixtures/gemini-creative.json
    - src/types/competitor.ts
    - src/types/viral.ts
  </read_first>

  <action>
**1. Update `src/types/database.ts` — Replace Recommendation and CreativeScript interfaces (per D-08, D-12):**

Replace the `Recommendation` interface with:
```typescript
/** Recomendacao estrategica gerada pela IA (per D-08) */
export interface Recommendation {
  action: string;
  reason: string;
  priority: 'alta' | 'media' | 'baixa';
  effort: 'alto' | 'medio' | 'baixo';
  expected_impact: string;
}
```

Replace the `CreativeScript` interface with:
```typescript
/** Roteiro criativo gerado pela IA (per D-12) */
export interface CreativeScript {
  title: string;
  format: string;
  estimated_duration_seconds: number;
  hook: { text: string; timing_seconds: number };
  body: { text: string; structure_notes: string };
  cta: { text: string; action: string };
  tone: string;
  inspiration_source: string;
}
```

Add a new interface for the structured synthesis sections (per D-02):
```typescript
/** Secao individual da sintese estrategica (per D-02) */
export interface SynthesisSection {
  title: string;
  summary: string;
  metrics: Record<string, string | number>;
  tags: string[];
  detailed_analysis: string;
}

/** Resultado completo da sintese estrategica (per D-03) */
export interface SynthesisOutput {
  marketOverview: SynthesisSection;
  competitorAnalysis: SynthesisSection;
  gapsAndOpportunities: SynthesisSection;
  viralPatterns: SynthesisSection;
  recommendations: Recommendation[];
}
```

Update the `Synthesis` interface — change `strategicOverview` type from `string` to `string` (it will store JSON-stringified `SynthesisOutput` sections object — the TEXT column in DB holds this). No type change needed here since the TEXT column stores a string. Keep it as is.

Update the `Database['public']['Tables']['synthesis']` Row/Insert/Update types — the `recommendations` and `creative_scripts` fields still use the same interface names (Recommendation[] and CreativeScript[]), just the interface shape changed. No changes needed to the Database type itself.

**2. Create `src/lib/validation/synthesisSchemas.ts` — Zod schemas for synthesis and creative output:**

```typescript
import { z } from 'zod';

/** Schema para secao individual da sintese (per D-02) */
export const synthesisSectionSchema = z.object({
  title: z.string().min(1),
  summary: z.string().min(1),
  metrics: z.record(z.string(), z.union([z.string(), z.number()])),
  tags: z.array(z.string()),
  detailed_analysis: z.string().min(1),
});

/** Schema para recomendacao individual (per D-08) */
export const recommendationSchema = z.object({
  action: z.string().min(1),
  reason: z.string().min(1),
  priority: z.enum(['alta', 'media', 'baixa']),
  effort: z.enum(['alto', 'medio', 'baixo']),
  expected_impact: z.string().min(1),
});

/** Schema completo do output de sintese (per D-03 Modo Rapido) */
export const synthesisOutputSchema = z.object({
  marketOverview: synthesisSectionSchema,
  competitorAnalysis: synthesisSectionSchema,
  gapsAndOpportunities: synthesisSectionSchema,
  viralPatterns: synthesisSectionSchema,
  recommendations: z.array(recommendationSchema).min(3).max(10),
});

/** Schema para roteiro criativo individual (per D-12) */
export const creativeScriptSchema = z.object({
  title: z.string().min(1),
  format: z.enum(['Reels', 'TikTok', 'YouTube Shorts', 'Stories']),
  estimated_duration_seconds: z.number().min(10).max(120),
  hook: z.object({
    text: z.string().min(1),
    timing_seconds: z.number().min(1).max(10),
  }),
  body: z.object({
    text: z.string().min(1),
    structure_notes: z.string().min(1),
  }),
  cta: z.object({
    text: z.string().min(1),
    action: z.string().min(1),
  }),
  tone: z.string().min(1),
  inspiration_source: z.string().min(1),
});

/** Schema completo do output de roteiros criativos */
export const creativeOutputSchema = z.object({
  scripts: z.array(creativeScriptSchema).min(3).max(5),
});
```

**3. Update `tests/fixtures/factories.ts` — Update `createRecommendation` and `createCreativeScript` factories to match new interfaces:**

Replace `createRecommendation` with:
```typescript
export const createRecommendation = (overrides?: Partial<Recommendation>): Recommendation => ({
  action: 'Criar serie semanal de antes/depois no Instagram',
  reason: 'Clinica Sorriso SP gera 891 likes por post de antes/depois vs 256 em posts educativos. Este formato gera 3.5x mais engajamento.',
  priority: 'alta',
  effort: 'baixo',
  expected_impact: 'Aumento de 200-300% no engajamento do Instagram em 30 dias',
  ...overrides,
});
```

Replace `createCreativeScript` with:
```typescript
export const createCreativeScript = (overrides?: Partial<CreativeScript>): CreativeScript => ({
  title: 'POV: Primeira consulta na clinica',
  format: 'Reels',
  estimated_duration_seconds: 30,
  hook: { text: 'Voce sabia que 70% das pessoas tem medo de dentista? Olha o que acontece quando elas chegam aqui...', timing_seconds: 3 },
  body: { text: 'Mostra a recepcao acolhedora, o ambiente moderno, o dentista explicando o procedimento com calma. Corta para o paciente sorrindo no final.', structure_notes: 'POV estilo "tour" com transicoes suaves. Use legendas grandes.' },
  cta: { text: 'Agende sua avaliacao gratuita pelo link na bio!', action: 'Link na bio para agendamento' },
  tone: 'acolhedor e profissional',
  inspiration_source: 'Padrao de hook provocativo com estatistica identificado em 4 videos virais do nicho',
  ...overrides,
});
```

**4. Create `tests/fixtures/gemini-synthesis-v2.json` — New fixture matching D-02 section format:**

```json
{
  "marketOverview": {
    "title": "Panorama do mercado odontologico em SP",
    "summary": "O mercado de clinicas odontologicas em SP e altamente competitivo com forte presenca digital. Taxa media de engajamento de 3.2% no Instagram supera a media do setor.",
    "metrics": { "concorrentes_analisados": 3, "engajamento_medio_instagram": 3.2, "investimento_meta_ads": "2 de 3 concorrentes ativos" },
    "tags": ["competitivo", "digital-first", "instagram-forte"],
    "detailed_analysis": "Os 3 concorrentes analisados apresentam presenca digital consolidada..."
  },
  "competitorAnalysis": {
    "title": "Analise detalhada dos concorrentes",
    "summary": "Clinica Sorriso SP lidera em engajamento social. DentalCare SP investe mais em Google Ads. OdontoPlus foca em preco.",
    "metrics": { "lider_social": "Clinica Sorriso SP", "lider_seo": "DentalCare SP", "maior_investimento_ads": "DentalCare SP" },
    "tags": ["sorriso-lider-social", "dentalcare-lider-seo"],
    "detailed_analysis": "Clinica Sorriso SP: 12.5k seguidores Instagram, engajamento 4.1%..."
  },
  "gapsAndOpportunities": {
    "title": "Lacunas e oportunidades identificadas",
    "summary": "Nenhum concorrente investe em TikTok de forma consistente. Keywords de cauda longa como 'clareamento dental zona sul sp' nao estao sendo disputadas.",
    "metrics": { "gap_tiktok": "0 de 3 com presenca ativa", "keywords_nao_disputadas": 5 },
    "tags": ["tiktok-oportunidade", "seo-cauda-longa"],
    "detailed_analysis": "A principal lacuna identificada e a ausencia de presenca ativa no TikTok..."
  },
  "viralPatterns": {
    "title": "Padroes de conteudo viral no nicho",
    "summary": "Videos com hook de pergunta provocativa e formato antes/depois dominam o engajamento. Duracao ideal entre 15-45 segundos.",
    "metrics": { "padrao_dominante": "pergunta provocativa", "duracao_ideal_segundos": "15-45", "tom_dominante": "educacional" },
    "tags": ["antes-depois", "pergunta-provocativa", "educacional"],
    "detailed_analysis": "Em 8 videos analisados, o padrao de hook mais frequente (5 de 8) e a pergunta provocativa..."
  },
  "recommendations": [
    {
      "action": "Criar serie semanal de antes/depois no Instagram",
      "reason": "Clinica Sorriso SP gera 891 likes por post de antes/depois vs 256 em posts educativos. Este formato gera 3.5x mais engajamento no nicho.",
      "priority": "alta",
      "effort": "baixo",
      "expected_impact": "Aumento de 200-300% no engajamento do Instagram em 30 dias"
    },
    {
      "action": "Iniciar presenca ativa no TikTok com 3 videos por semana",
      "reason": "Nenhum dos 3 concorrentes tem presenca ativa no TikTok. Clinica Sorriso SP tem 8.400 seguidores abandonados. Oportunidade de first-mover no nicho local.",
      "priority": "alta",
      "effort": "medio",
      "expected_impact": "Capturar audiencia jovem (18-30) nao atendida pelos concorrentes em 60 dias"
    },
    {
      "action": "Criar paginas de conteudo para keywords de cauda longa: 'clareamento dental zona sul sp', 'implante dentario parcelado sp', 'dentista emergencia sp'",
      "reason": "DentalCare SP ranqueia para 'dentista sp' (alta concorrencia, CPC R$ 8.50). Keywords de cauda longa tem CPC 60% menor e conversao 2x maior.",
      "priority": "media",
      "effort": "medio",
      "expected_impact": "Reducao de 40-60% no custo por lead via busca organica em 90 dias"
    },
    {
      "action": "Otimizar Google Meu Negocio com fotos profissionais e responder todas as avaliacoes",
      "reason": "Clinica Sorriso SP tem 4.2 estrelas (89 avaliacoes) mas nao responde 67% das avaliacoes negativas. OdontoPlus tem 4.7 (156 avaliacoes) com 95% de respostas.",
      "priority": "media",
      "effort": "baixo",
      "expected_impact": "Melhoria de 0.3-0.5 pontos na avaliacao media e aumento de 20% em visitas ao perfil"
    },
    {
      "action": "Testar Meta Ads com formato video curto (15-30s) usando hook de pergunta provocativa",
      "reason": "2 de 3 concorrentes usam apenas imagens estaticas nos anuncios. Videos com hook provocativo geram 4x mais cliques no nicho odontologico segundo padroes virais analisados.",
      "priority": "media",
      "effort": "alto",
      "expected_impact": "Reducao de 30-50% no custo por clique comparado a imagens estaticas"
    }
  ]
}
```

**5. Create `tests/fixtures/gemini-creative-v2.json` — New fixture matching D-12 script format:**

```json
{
  "scripts": [
    {
      "title": "POV: Primeira consulta na clinica",
      "format": "Reels",
      "estimated_duration_seconds": 30,
      "hook": { "text": "Voce sabia que 70% das pessoas tem medo de dentista? Olha o que acontece quando elas chegam aqui...", "timing_seconds": 3 },
      "body": { "text": "Mostra a recepcao acolhedora, o ambiente moderno, o dentista explicando o procedimento com calma. Corta para o paciente sorrindo no final.", "structure_notes": "POV estilo tour com transicoes suaves. Legendas grandes para visualizacao sem audio." },
      "cta": { "text": "Agende sua avaliacao gratuita pelo link na bio!", "action": "Link na bio para agendamento online" },
      "tone": "acolhedor e profissional",
      "inspiration_source": "Padrao de hook com estatistica provocativa identificado em 5 de 8 videos virais do nicho"
    },
    {
      "title": "3 sinais de que voce precisa de um clareamento",
      "format": "TikTok",
      "estimated_duration_seconds": 45,
      "hook": { "text": "Se voce percebe ISSO no espelho, precisa assistir ate o final", "timing_seconds": 2 },
      "body": { "text": "Sinal 1: dentes amarelados mesmo escovando bem. Sinal 2: sorriso apagado nas fotos. Sinal 3: voce evita sorrir abertamente. A boa noticia: clareamento resolve em 1 sessao.", "structure_notes": "Formato lista numerada com cortes rapidos a cada sinal. Texto na tela reforca cada ponto." },
      "cta": { "text": "Quer saber o preco? Comenta EU QUERO que mando no direct!", "action": "Comentario para DM automatico" },
      "tone": "educacional e direto",
      "inspiration_source": "Formula recorrente 'lista de sinais + solucao' encontrada em 3 videos virais do nicho"
    },
    {
      "title": "Antes e depois REAL de implante",
      "format": "Reels",
      "estimated_duration_seconds": 60,
      "hook": { "text": "Ela perdeu o dente da frente num acidente. Olha como ficou depois...", "timing_seconds": 3 },
      "body": { "text": "Mostra foto do antes com depoimento do paciente. Processo simplificado do implante em 3 etapas. Reveal do resultado final com reacao genuina.", "structure_notes": "Formato antes/depois com storytelling. Musica emocional de fundo. Slow motion no reveal." },
      "cta": { "text": "Implante a partir de R$ 3.500 em ate 12x. Link na bio pra agendar.", "action": "Link na bio com preco e parcelamento" },
      "tone": "emocional e inspirador",
      "inspiration_source": "Padrao de antes/depois com depoimento real dominante nos videos com maior engajamento"
    }
  ]
}
```
  </action>

  <verify>
    <automated>cd "C:/Users/biela/OneDrive/Documentos/github/lupai" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - src/types/database.ts contains `action: string` in Recommendation interface (not `title: string` or `description: string`)
    - src/types/database.ts contains `priority: 'alta' | 'media' | 'baixa'` in Recommendation interface
    - src/types/database.ts contains `effort: 'alto' | 'medio' | 'baixo'` in Recommendation interface
    - src/types/database.ts contains `expected_impact: string` in Recommendation interface
    - src/types/database.ts contains `hook: { text: string; timing_seconds: number }` in CreativeScript interface
    - src/types/database.ts contains `body: { text: string; structure_notes: string }` in CreativeScript interface
    - src/types/database.ts contains `cta: { text: string; action: string }` in CreativeScript interface
    - src/types/database.ts contains `export interface SynthesisSection`
    - src/types/database.ts contains `export interface SynthesisOutput`
    - src/lib/validation/synthesisSchemas.ts contains `export const synthesisOutputSchema`
    - src/lib/validation/synthesisSchemas.ts contains `export const creativeOutputSchema`
    - src/lib/validation/synthesisSchemas.ts contains `export const recommendationSchema`
    - src/lib/validation/synthesisSchemas.ts contains `export const creativeScriptSchema`
    - src/lib/validation/synthesisSchemas.ts contains `z.enum(['alta', 'media', 'baixa'])`
    - src/lib/validation/synthesisSchemas.ts contains `z.enum(['alto', 'medio', 'baixo'])`
    - src/lib/validation/synthesisSchemas.ts contains `z.enum(['Reels', 'TikTok', 'YouTube Shorts', 'Stories'])`
    - tests/fixtures/factories.ts createRecommendation contains `action:` and `reason:` (not `title:` and `description:`)
    - tests/fixtures/factories.ts createCreativeScript contains `hook: {` (nested object, not flat string)
    - tests/fixtures/gemini-synthesis-v2.json exists and contains `marketOverview`
    - tests/fixtures/gemini-creative-v2.json exists and contains `scripts`
    - `npx tsc --noEmit` exits with 0 errors
  </acceptance_criteria>

  <done>Updated Recommendation interface uses action/reason/priority(PT-BR)/effort/expected_impact. Updated CreativeScript uses nested hook/body/cta objects with metadata. Zod schemas validate both. Factories and fixtures match new shapes. TypeScript compiles cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upgrade synthesize.ts and creative.ts with Zod structured output, dynamic token truncation, add prompts and tests</name>
  <files>src/lib/ai/synthesize.ts, src/lib/ai/creative.ts, src/lib/ai/prompts.ts, tests/unit/synthesize-ai.test.ts, tests/unit/creative-ai.test.ts</files>

  <read_first>
    - src/lib/ai/synthesize.ts
    - src/lib/ai/creative.ts
    - src/lib/ai/prompts.ts
    - src/lib/ai/hbc-extraction.ts
    - src/lib/ai/viral-patterns.ts
    - src/lib/validation/extractionSchemas.ts
    - src/lib/validation/synthesisSchemas.ts (created in Task 1)
    - src/types/database.ts (updated in Task 1)
    - tests/fixtures/gemini-synthesis-v2.json (created in Task 1)
    - tests/fixtures/gemini-creative-v2.json (created in Task 1)
    - tests/fixtures/factories.ts (updated in Task 1)
  </read_first>

  <behavior>
    - Test: synthesizeAnalysis with valid competitors and viral data returns SynthesisOutput with all 4 sections and 3+ recommendations
    - Test: synthesizeAnalysis recommendations have action/reason/priority/effort/expected_impact fields (not old title/description/category)
    - Test: synthesizeAnalysis with empty competitors still returns (generates from available data)
    - Test: synthesizeAnalysis returns null when Gemini returns invalid JSON (validateOrNull pattern)
    - Test: truncateContextIfNeeded reduces viral content to ViralPatterns-only when token count exceeds threshold (per D-29)
    - Test: generateCreativeScripts with viral patterns returns 3-5 scripts with nested hook/body/cta
    - Test: generateCreativeScripts each script has format (Reels|TikTok|YouTube Shorts|Stories) and estimated_duration_seconds
    - Test: generateCreativeScripts without viral patterns (null) still returns scripts using competitor analysis as fallback (per D-14)
    - Test: generateCreativeScripts returns null when Gemini returns invalid JSON
  </behavior>

  <action>
**1. Replace `SYNTHESIZE_PROMPT` in `src/lib/ai/prompts.ts` with the D-06/D-07/D-34 compliant prompt:**

Replace the existing `SYNTHESIZE_PROMPT` with:
```typescript
/** Prompt para sintese estrategica (per D-01 to D-09, D-34) */
export const SYNTHESIZE_PROMPT = `Voce e um consultor de marketing digital experiente e analitico.
Analise os dados coletados sobre concorrentes, mercado e conteudo viral, e gere uma sintese estrategica completa.

Gere EXATAMENTE estas secoes, cada uma com os campos: title, summary (2-3 linhas), metrics (pares chave-valor com dados reais), tags (array de strings), detailed_analysis (markdown, maximo 2 paragrafos):

1. marketOverview: Panorama geral do mercado e posicionamento dos players
2. competitorAnalysis: Analise detalhada de cada concorrente com metricas especificas
3. gapsAndOpportunities: Lacunas no mercado e oportunidades nao exploradas
4. viralPatterns: Padroes de conteudo viral identificados e como aplica-los

Gere tambem 5 a 8 recomendacoes estrategicas. Cada recomendacao DEVE ter:
- action: O que fazer (acao especifica e executavel)
- reason: Por que fazer, citando dados ESPECIFICOS (nome do concorrente, numero exato, exemplo concreto)
- priority: "alta", "media" ou "baixa"
- effort: "alto", "medio" ou "baixo"
- expected_impact: Impacto esperado com estimativa quantitativa

REGRAS ABSOLUTAS:
- Toda recomendacao DEVE citar dados especificos: nome do concorrente, numero exato, exemplo concreto
- Recomendacoes genericas como "melhore seu SEO" ou "invista em conteudo" serao REJEITADAS
- Padrao correto: "Seus concorrentes ranqueiam pra 'whey protein isolado' e voce nao tem conteudo sobre isso — crie uma pagina focada nessa keyword"
- Ordene recomendacoes por relacao impacto/esforco (maior impacto com menor esforco primeiro)
- Todos os textos em portugues brasileiro
- Responda APENAS em JSON conforme o schema fornecido`;
```

Replace the existing `CREATIVE_PROMPT` with:
```typescript
/** Prompt para modelagem de criativos (per D-10 to D-14, D-34) */
export const CREATIVE_PROMPT = `Voce e um roteirista de conteudo viral para redes sociais, especialista em videos curtos.
Com base nos padroes de conteudo viral do nicho e dados de concorrentes, gere roteiros de video adaptados.

Gere de 3 a 5 roteiros. Cada roteiro DEVE ter:
- title: Titulo descritivo do roteiro
- format: Formato recomendado (exatamente um de: "Reels", "TikTok", "YouTube Shorts", "Stories")
- estimated_duration_seconds: Duracao total estimada (entre 10 e 120 segundos)
- hook: { text: Frase de abertura que prende atencao, timing_seconds: Duracao do hook em segundos (1-10) }
- body: { text: Desenvolvimento do conteudo, structure_notes: Notas de estrutura e producao }
- cta: { text: Chamada para acao final, action: Acao esperada do espectador }
- tone: Tom de voz (ex: "educacional e direto", "humoristico", "emocional e inspirador")
- inspiration_source: Qual padrao viral inspirou este roteiro (referencie padroes dos dados fornecidos)

REGRAS:
- Roteiros devem ser ADAPTADOS ao nicho especifico, nao genericos
- Use os padroes de hook, CTA e formulas recorrentes dos dados virais fornecidos como base
- Varie os formatos (nao gere todos para a mesma plataforma)
- Varie os tons (misture educacional, emocional, humoristico)
- Todos os textos em portugues brasileiro
- Responda APENAS em JSON conforme o schema fornecido`;
```

**2. Rewrite `src/lib/ai/synthesize.ts` — Upgrade to Zod structured output pattern (per D-05):**

Follow the exact pattern from `hbc-extraction.ts` and `viral-patterns.ts`:

```typescript
import { GoogleGenAI } from '@google/genai';
import { zodToJsonSchema } from 'zod-to-json-schema';

import type { Competitor } from '@/types/competitor';
import type { ViralContent } from '@/types/viral';
import type { ViralPatterns } from '@/types/viral';
import type { SynthesisOutput } from '@/types/database';
import { synthesisOutputSchema } from '@/lib/validation/synthesisSchemas';
import { validateOrNull } from '@/lib/validation/extractionSchemas';
import { SYNTHESIZE_PROMPT } from './prompts';
```

The function signature changes to accept viralPatterns explicitly:
```typescript
export const synthesizeAnalysis = async (input: {
  niche: string;
  segment: string;
  region: string;
  competitors: Competitor[];
  viralContent: ViralContent[];
  viralPatterns: ViralPatterns | null;
}): Promise<SynthesisOutput | null> => {
```

Add a helper function `assembleCompetitorContext` that pre-summarizes competitor data (per D-28, D-29, D-30):
- For each competitor: extract name, websiteUrl, positioning, top 5 keywords, estimatedTraffic, instagram (followers, engagementRate, postingFrequency), tiktok (followers, engagementRate, postingFrequency), activeAds count, hasGoogleAds, top 5 paidKeywords, gmb (rating, reviewCount)
- Skip null fields

Add a helper function `assembleViralContext` that summarizes viral data:
- For viralPatterns: include the full object (hookPatterns, bodyStructures, ctaPatterns, dominantTone, bestPerformingDuration, recurringFormulas)
- For viralContent: include top 5 items with caption + hookBodyCta summary + engagement metrics (NOT full transcriptions — per D-29)

Add a **dynamic token budget truncation** helper function `truncateContextIfNeeded` (per D-29). This is a separate exported function per CLAUDE.md function-size rule (<30 lines):

```typescript
/** Trunca contexto dinamicamente se exceder o orcamento de tokens do Gemini (per D-29) */
export const truncateContextIfNeeded = async (
  fullPrompt: string,
  competitorContext: unknown,
  viralContext: { viralPatterns: ViralPatterns | null; viralContent: unknown[] },
  threshold: number = 200_000
): Promise<{ competitorContext: unknown; viralContext: unknown }> => {
```

Implementation:
- Use `genai.models.countTokens({ model: 'gemini-2.0-flash', contents: fullPrompt })` to count the token usage of the assembled prompt
- If `tokenCount.totalTokens` is **below** the threshold (200,000 tokens): return contexts unchanged
- If **above** threshold: apply a second pass that:
  1. Keeps `competitorContext` unchanged (highest priority per D-29: competitor core data > viral patterns > individual video details)
  2. Keeps `viralPatterns` from the viral context (second priority)
  3. Drops individual `viralContent` items entirely (lowest priority — individual video details)
  4. Returns the reduced contexts
- Log a warning: `console.warn('Aviso: contexto truncado de ${tokenCount.totalTokens} tokens para caber no orcamento do Gemini')`

The main function body:
- Assemble context via `assembleCompetitorContext` and `assembleViralContext` helpers
- Build the initial prompt string: `${SYNTHESIZE_PROMPT}\n\nNicho: ${input.niche}\nSegmento: ${input.segment}\nRegiao: ${input.region}\n\nDados dos concorrentes:\n${JSON.stringify(competitorContext)}\n\nPadroes virais:\n${JSON.stringify(viralContext)}`
- **Call `truncateContextIfNeeded(fullPrompt, competitorContext, viralContext)`** — if truncation occurred, rebuild the prompt with the reduced contexts
- Call Gemini with `responseMimeType: 'application/json'` and `responseJsonSchema: zodToJsonSchema(synthesisOutputSchema)` and `maxOutputTokens: 8192`
- Parse response.text, run through `validateOrNull(synthesisOutputSchema, parsed)`
- On error: `console.warn('Aviso: falha na sintese estrategica: ...')` and return null

**3. Rewrite `src/lib/ai/creative.ts` — Upgrade to Zod structured output pattern (per D-05):**

Same pattern. Function signature changes to accept viralPatterns and competitor data for D-14 fallback:
```typescript
export const generateCreativeScripts = async (input: {
  niche: string;
  segment: string;
  viralContent: ViralContent[];
  viralPatterns: ViralPatterns | null;
  competitors: Competitor[];
}): Promise<CreativeScript[] | null> => {
```

Build context:
- If `viralPatterns` is non-null: include hookPatterns, ctaPatterns, recurringFormulas, dominantTone, bestPerformingDuration (per D-11, D-13)
- If `viralPatterns` is null: include competitor summary instead (per D-14 fallback) with a note "Sem padroes virais disponiveis. Gere roteiros baseados na analise dos concorrentes e melhores praticas do nicho."
- Include top 3 viralContent with HBC breakdowns for specific examples

Call Gemini with `creativeOutputSchema`, parse, validate. Extract `result.scripts` from the validated output. Return the scripts array or null.

**4. Create `tests/unit/synthesize-ai.test.ts`:**

Mock `@google/genai` using `vi.mock` with class syntax (per Phase 3 pattern). Mock `GoogleGenAI` constructor and `genai.models.generateContent` to return the fixture data from `gemini-synthesis-v2.json`. Also mock `genai.models.countTokens` to return `{ totalTokens: 50000 }` by default (below threshold — no truncation).

Tests:
- `synthesizeAnalysis retorna sintese estruturada com todas as secoes`: Call with factory competitors (with websiteData, seoData, socialData, metaAdsData) and viralContent. Assert result has `marketOverview`, `competitorAnalysis`, `gapsAndOpportunities`, `viralPatterns` keys. Assert each section has `title`, `summary`, `metrics`, `tags`, `detailed_analysis`.
- `recomendacoes tem campos action/reason/priority/effort/expected_impact`: Assert each recommendation has all 5 fields. Assert priority is one of `alta|media|baixa`. Assert effort is one of `alto|medio|baixo`.
- `retorna null quando Gemini retorna JSON invalido`: Mock Gemini to return `{ invalid: true }`. Assert result is null.
- `funciona com array vazio de concorrentes`: Call with empty competitors array. Assert does not throw, returns result (Gemini still generates from niche context).
- `truncateContextIfNeeded reduz contexto viral quando tokens excedem limite (per D-29)`: Mock `genai.models.countTokens` to return `{ totalTokens: 250000 }` (above 200k threshold). Call `truncateContextIfNeeded` with full context including viralContent items. Assert returned `viralContext` has `viralPatterns` preserved but `viralContent` is an empty array. Assert `console.warn` was called with truncation message.
- `truncateContextIfNeeded preserva contexto quando abaixo do limite`: Mock `genai.models.countTokens` to return `{ totalTokens: 50000 }`. Call `truncateContextIfNeeded`. Assert contexts returned unchanged.

**5. Create `tests/unit/creative-ai.test.ts`:**

Same mock pattern. Mock returns `gemini-creative-v2.json` fixture.

Tests:
- `generateCreativeScripts retorna roteiros com hook/body/cta estruturados`: Call with viralContent and viralPatterns. Assert result is array of 3+. Assert each script has `hook.text`, `hook.timing_seconds`, `body.text`, `body.structure_notes`, `cta.text`, `cta.action`.
- `cada roteiro tem formato e duracao estimada`: Assert each script has `format` matching one of `Reels|TikTok|YouTube Shorts|Stories`. Assert `estimated_duration_seconds` between 10-120.
- `gera roteiros sem padroes virais usando fallback de concorrentes (per D-14)`: Call with `viralPatterns: null` and competitors. Assert result is not null. Verify the Gemini call included competitor data in the prompt (check `generateContent` mock call args contain competitor names).
- `retorna null quando Gemini retorna JSON invalido`: Mock invalid return. Assert result is null.
  </action>

  <verify>
    <automated>cd "C:/Users/biela/OneDrive/Documentos/github/lupai" && npx vitest run tests/unit/synthesize-ai.test.ts tests/unit/creative-ai.test.ts --reporter verbose 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - src/lib/ai/synthesize.ts contains `import { synthesisOutputSchema } from '@/lib/validation/synthesisSchemas'`
    - src/lib/ai/synthesize.ts contains `zodToJsonSchema(synthesisOutputSchema)`
    - src/lib/ai/synthesize.ts contains `validateOrNull(synthesisOutputSchema`
    - src/lib/ai/synthesize.ts contains `responseMimeType: 'application/json'`
    - src/lib/ai/synthesize.ts contains `viralPatterns: ViralPatterns | null`
    - src/lib/ai/synthesize.ts contains `Promise<SynthesisOutput | null>`
    - src/lib/ai/synthesize.ts contains `export const truncateContextIfNeeded` (per D-29)
    - src/lib/ai/synthesize.ts contains `countTokens` call (per D-29)
    - src/lib/ai/synthesize.ts contains `200_000` or `200000` as token threshold (per D-29)
    - src/lib/ai/creative.ts contains `import { creativeOutputSchema } from '@/lib/validation/synthesisSchemas'`
    - src/lib/ai/creative.ts contains `zodToJsonSchema(creativeOutputSchema)`
    - src/lib/ai/creative.ts contains `validateOrNull(creativeOutputSchema`
    - src/lib/ai/creative.ts contains `viralPatterns: ViralPatterns | null`
    - src/lib/ai/creative.ts contains `Promise<CreativeScript[] | null>`
    - src/lib/ai/prompts.ts contains `Recomendacoes genericas como "melhore seu SEO"` (anti-generic enforcement per D-07)
    - src/lib/ai/prompts.ts contains `action: O que fazer` in SYNTHESIZE_PROMPT
    - src/lib/ai/prompts.ts CREATIVE_PROMPT contains `inspiration_source`
    - tests/unit/synthesize-ai.test.ts has test for `truncateContextIfNeeded` (per D-29)
    - tests/unit/synthesize-ai.test.ts exits 0 (all tests pass)
    - tests/unit/creative-ai.test.ts exits 0 (all tests pass)
    - `npx tsc --noEmit` exits with 0 errors
  </acceptance_criteria>

  <done>synthesizeAnalysis returns structured SynthesisOutput with 4 sections and recommendations using Zod validation. Dynamic token budget truncation via truncateContextIfNeeded drops individual video details when context exceeds 200k tokens (per D-29), preserving competitor data and viral patterns. generateCreativeScripts returns 3-5 scripts with nested hook/body/cta using Zod validation. Both use zodToJsonSchema + validateOrNull pattern. Both handle Gemini failures gracefully (return null). Creative scripts fall back to competitor data when viral patterns unavailable. All tests pass.</done>
</task>

</tasks>

<verification>
Run full test suite to verify no regressions:
```bash
npm test
```

Verify TypeScript compilation:
```bash
npx tsc --noEmit
```
</verification>

<success_criteria>
- Recommendation and CreativeScript types updated with new fields (D-08, D-12)
- SynthesisSection and SynthesisOutput types exist (D-02, D-03)
- Zod schemas for synthesis and creative output exist and validate correctly
- synthesizeAnalysis upgraded from raw JSON.parse to Zod + zodToJsonSchema + validateOrNull
- synthesizeAnalysis includes truncateContextIfNeeded for dynamic token budget management (D-29)
- generateCreativeScripts upgraded similarly
- Prompts enforce data-specific recommendations (D-06, D-07) and PT-BR output (D-34)
- Creative scripts accept viralPatterns and fall back to competitor data (D-14)
- All unit tests pass including truncation tests
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-synthesis-creative-modeling/07-01-SUMMARY.md`
</output>
